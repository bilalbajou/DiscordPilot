# DiscordPilot MCP
> MCP server that connects Claude Code to Discord — manage servers,
> channels, members, roles and messages directly from your terminal.

## Setup
1. cp .env.example .env
2. Ajoute ton DISCORD_BOT_TOKEN dans .env
3. pip install -r requirements.txt
4. python src/server.py
